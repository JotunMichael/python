git rm --cached /path/to/file || git rm --cached /path/to/file
docker build .
docker-compose build
docker-compose run app sh -c "django-admin.py startproject app ."
git add .
git commit -a
message pass e.g. Setup docker and Django project :wq